{% extends 'base.html.twig' %}

{% block title %}Hello PanierController!{% endblock %}

{% block body %}

{% if items | length > 0 %}

<table class = "table">
<thead>
<tr>
    <th>nam</th>
    {# <th>apparence</th> #}
    <th>prix</th>
    <th>Quantité</th>
    <th>Total</th>
    <th></th>
</tr>

</thead>
{% for item in items %}
<tbody>
<tr>
    <td>{{item.article.nam}}</td>
    {# <td ><img src="{{item.article.image}}" class ='card-img-top'></td> #}
    <td>{{item.article.price}}</td>
    <td>{{item.quantity}}</td>
    <td>{{item.quantity * item.article.price }}</td>
    <td><a href="{{path('panier_delete',{'id' : item.article.id })}}" class="btn btn-danger btn-sm"> supprimer</a></td>
</tr>
{% endfor %}
</tbody>

 <tfoot>
 <tr>
     <td colspan= "3" class='text-right'>Total:</td>
     <td>{{total}}  </td>
     <td><a href="{{path('panier_trensaction')}}"> passer commande</a></td>
 </tr>
 </tfoot>

</table>



{% else %}
    
    <h3>Panier vide!</h3>

{% endif %}

{% endblock %}
