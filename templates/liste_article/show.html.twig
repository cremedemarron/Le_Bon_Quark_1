{% extends 'base.html.twig' %}

{% block title %}
	{{ article.nam }}
{% endblock %}

{% block body %}
	{# <h1>{{ article.nam }}</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ article.id }}</td>
			</tr>
			<tr>
				<th>Nam</th>
				<td>{{ article.nam }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ article.description }}</td>
			</tr>
			<tr>
				<th>Localisation</th>
				<td>{{ article.localisation }}</td>
			</tr>
			<tr>
				<th>Price</th>
				<td>{{ article.price }}</td>
			</tr>
			<tr>
				<th>Weight</th>
				<td>{{ article.weight }}</td>
			</tr>
			<tr>
				<th>image</th>
				<td>{{ article.image }}</td>
			</tr>

			<th>auteure_id
			</th>
			<td>{{ article.auteure }}</td>


			<tr>
				<th>category</th>
				<td>{{ article.category }}</td>
			</tr>

		</tbody>
	</table>

	<a href="{{ path('liste_article_index') }}">
		liste des articles</a>

	{% if  is_granted('ROLE_USER') %}
		<a href="{{path('panier_add',{'id': article.id})}}">
			ajouter au panier</a>
	{% endif %}
	{% if app.user.psedo is defined %}
		{% if article.auteure == app.user.psedo %}

			<a href="{{ path('liste_article_edit', {'id': article.id}) }}">edit</a>

			{{ include('liste_article/_delete_form.html.twig') }}
		{% endif  %}
	{% endif  %} #}

<html lang="en">

<head>
  <title>{{ article.nam }}</title>
  <link href="https://fonts.googleapis.com/css?family=Bentham|Playfair+Display|Raleway:400,500|Suranna|Trocchi" rel="stylesheet">
</head>

<body>
  <div class="wrapper">
    <div class="product-img">
      <img src="{{article.image}}" height="420" width="327">
    </div>
    <div class="product-info">
      <div class="product-text">
        <h1>{{ article.nam }}</h1>
        <h2>par :{{article.auteure}}</h2>
        <p>{{article.description}} </p>
      </div>
      <div class="product-price-btn">
        <p><span>prix: {{article.price}}</span> ξ</p>
		<br>
		<br>
		<br>
		<br>
		<br>
		<br>
   
		<div class='row justify-content-start'>
		{% if  is_granted('ROLE_USER') %}
		<div class='col-md-6 text-left'>
		<button type="button"><a href="{{path('panier_add',{'id': article.id})}}" class="lien">
			ajouter au panier</a></button>
			</div>
	{% endif %}
	{% if app.user.psedo is defined %}
		{% if article.auteure == app.user.psedo %}
<div class='col-md-6'>
			<button type="button"><a href="{{ path('liste_article_edit', {'id': article.id}) }} " class="lien">modifier</a></button>

</div>
		<div class='col-md-9 '>
			{{ include('liste_article/_delete_form.html.twig') }}
			</div>
</div>
		{% endif  %}
	{% endif  %} 

</div>

      </div>
    </div>
  </div>
	<a href="{{ path('liste_article_index') }}" class="btn btn-success">
		liste des articles</a>
</body>

</html>

		
{% endblock %}
